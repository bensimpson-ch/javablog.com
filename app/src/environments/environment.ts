export const environment = {
  production: false,
  apiBasePath: 'http://localhost:8080/v1',
  keycloak: {
    issuer: 'https://keycloak.javablog.com/realms/javablog',
    clientId: 'javablog-client'
  }
};
